# 🚀 Как запустить бота

## Способ 1: Автоматический запуск (рекомендуется)

### Для Windows:

1. **Двойной клик на файл `run.bat`**
   - Скрипт сам проверит всё необходимое
   - Создаст виртуальное окружение (если нужно)
   - Установит зависимости
   - Запустит бота

2. **Или через командную строку:**
   ```cmd
   run.bat
   ```

## Способ 2: Ручной запуск

### Шаг 1: Проверьте настройки

Убедитесь, что файл `.env` существует и содержит правильные данные:

```env
BOT_TOKEN=ваш_токен_от_BotFather
ADMIN_IDS=ваш_telegram_id
DATABASE_URL=sqlite:///./lunch_bot.db
```

Если файла `.env` нет, создайте его из примера:
```cmd
copy env.example .env
```

### Шаг 2: Создайте виртуальное окружение

```cmd
python -m venv venv
```

### Шаг 3: Активируйте виртуальное окружение

```cmd
venv\Scripts\activate
```

После активации в начале строки появится `(venv)`

### Шаг 4: Установите зависимости

```cmd
pip install -r requirements.txt
```

### Шаг 5: Инициализируйте базу данных

```cmd
python scripts\init_db.py
```

### Шаг 6: Запустите бота

```cmd
python main.py
```

Или через скрипт:
```cmd
python run.py
```

## ✅ Проверка работы

1. В консоли должно появиться сообщение: `🚀 Бот запущен и готов к работе!`

2. Откройте Telegram и найдите вашего бота

3. Отправьте команду `/start`

4. Бот должен ответить приветственным сообщением

## 📝 Логи

Логи сохраняются в папке `logs/` в файлах вида `bot_YYYY-MM-DD.log`

## ⚠️ Если что-то не работает

1. **Проверьте, что Python установлен:**
   ```cmd
   python --version
   ```
   Должна быть версия 3.10 или выше

2. **Проверьте настройки в `.env`:**
   - `BOT_TOKEN` должен быть правильным
   - `ADMIN_IDS` должен содержать ваш Telegram ID

3. **Проверьте логи:**
   - Откройте папку `logs/`
   - Посмотрите последний файл лога
   - Найдите ошибки (они помечены как ERROR)

4. **Переустановите зависимости:**
   ```cmd
   pip install -r requirements.txt --upgrade
   ```

## 🔄 Перезапуск бота

1. Остановите бота (нажмите `Ctrl+C` в окне с ботом)

2. Запустите снова через `run.bat` или `python main.py`

## 🆘 Проблемы

Если бот не отвечает на команду `/start`:
- Проверьте, что бот запущен (окно не закрыто)
- Проверьте логи на наличие ошибок
- Убедитесь, что BOT_TOKEN правильный


