# Telegram-бот для предварительного заказа обедов в офисе

Бот для автоматизации процесса предварительного заказа обедов в офисе. Решает проблему неудобного процесса заказа через личные сообщения и гугл-таблицы, автоматизируя сбор и структурирование заказов.

## 🚀 Возможности

### Для сотрудников:
- 🔐 **Авторизация по Telegram-ID** - автоматическая авторизация при первом использовании
- 📋 **Просмотр меню** - выбор офиса/кафе и просмотр доступного меню
- 🛒 **Оформление заказа** - выбор блюд, количества и времени доставки
- ✏️ **Управление заказом** - редактирование и отмена заказов (до дедлайна)
- 📊 **История заказов** - просмотр всех своих заказов

### Для офис-менеджеров:
- 👥 **Загрузка сотрудников** - добавление сотрудников (Telegram-ID, имя, офис) через админ-панель
- ⏰ **Управление дедлайнами** - настройка сроков заказа для каждого кафе и офиса
- 📅 **Управление меню** - загрузка меню на конкретные даты для каждого кафе
- 🏢 **Управление офисами и кафе** - добавление, редактирование и настройка офисов и кафе
- 📊 **Просмотр заказов** - просмотр всех заказов с фильтрацией по пользователю, статусу и поиском
- 📈 **Отчёты по кафе** - автоматическая генерация отчётов по кафе с группировкой заказов по сотрудникам
- 🚫 **Запрет изменений** - автоматический запрет изменений заказов после дедлайна
- 📥 **Экспорт данных** - экспорт отчетов в Excel и CSV, включая детальные отчеты по кафе

## 📋 Установка

### Быстрый старт

См. **[QUICK_START.md](QUICK_START.md)** для быстрого развертывания за 5 минут.

### Требования

- Python 3.10+
- Telegram Bot Token (получить у @BotFather)
- База данных: SQLite (для разработки) или PostgreSQL (для продакшена)

### Варианты развертывания

#### 1. Локальное развертывание (для разработки)

```bash
# Автоматическое развертывание
# Linux/Mac:
bash scripts/deploy.sh

# Windows:
scripts\deploy.bat
```

Или вручную:

1. **Создайте виртуальное окружение:**
```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
venv\Scripts\activate    # Windows
```

2. **Установите зависимости:**
```bash
pip install -r requirements.txt
```

3. **Настройте `.env` файл:**
```bash
cp env.example .env
# Заполните BOT_TOKEN и ADMIN_IDS
```

4. **Инициализируйте базу данных:**
```bash
python scripts/init_db.py
```

5. **Запустите бота:**
```bash
python main.py
```

#### 2. Docker (рекомендуется для продакшена)

```bash
# 1. Создайте .env файл
cp env.example .env

# 2. Настройте .env для PostgreSQL
# DATABASE_URL=postgresql+asyncpg://botuser:changeme@postgres:5432/quicklunch_bot

# 3. Запустите
docker-compose up -d
```

#### 3. Облачные платформы

- **Railway** - см. `DEPLOYMENT_GUIDE.md`
- **Heroku** - см. `DEPLOYMENT_GUIDE.md`
- **Render** - см. `DEPLOYMENT_GUIDE.md`
- **VPS** - см. `DEPLOYMENT_GUIDE.md`

📚 **Подробные инструкции:** См. [DEPLOYMENT_GUIDE.md](DEPLOYMENT_GUIDE.md)

## 📁 Структура проекта

```
TGBot/
├── main.py                      # Точка входа приложения
├── requirements.txt             # Зависимости проекта
├── .gitignore                   # Игнорируемые файлы Git
├── env.example                  # Пример конфигурации
├── README.md                    # Документация
├── TODO.md                      # Список задач
│
├── config/                      # Конфигурация
│   ├── __init__.py
│   └── settings.py             # Настройки приложения
│
├── handlers/                    # Обработчики команд
│   ├── __init__.py
│   ├── start.py                # Команда /start
│   ├── help.py                 # Команда /help
│   ├── menu.py                 # Работа с меню и создание заказов
│   ├── orders.py               # Работа с заказами
│   ├── edit_order.py           # Редактирование заказов
│   ├── admin.py                # Админ-панель
│   └── callbacks.py            # Обработка callback-запросов
│
├── models/                      # Модели базы данных
│   ├── __init__.py
│   ├── user.py                 # Модель пользователя
│   ├── dish.py                 # Модель блюда
│   ├── order.py                # Модели заказа
│   └── menu.py                 # Модель меню
│
├── database/                    # Работа с БД
│   ├── __init__.py
│   ├── base.py                 # Базовый класс моделей
│   └── database.py             # Подключение и инициализация БД
│
├── services/                    # Бизнес-логика
│   ├── __init__.py
│   ├── user_service.py         # Работа с пользователями
│   ├── menu_service.py         # Работа с меню
│   ├── menu_management_service.py  # Управление меню
│   ├── order_service.py        # Работа с заказами
│   ├── notification_service.py # Уведомления
│   ├── report_service.py       # Генерация отчетов
│   └── scheduler_service.py    # Планировщик задач
│
├── middleware/                  # Middleware
│   ├── __init__.py
│   ├── logging_middleware.py   # Логирование запросов
│   └── error_middleware.py     # Обработка ошибок
│
├── utils/                       # Утилиты
│   ├── __init__.py
│   ├── validators.py           # Валидация данных
│   ├── formatters.py           # Форматирование вывода
│   └── export_service.py      # Экспорт данных
│
├── scripts/                     # Скрипты
│   ├── __init__.py
│   └── init_db.py              # Инициализация БД с тестовыми данными
│
├── tests/                       # Тесты
│   ├── __init__.py
│   ├── test_validators.py     # Тесты валидации
│   ├── test_formatters.py     # Тесты форматирования
│   └── test_models.py         # Тесты моделей
│
└── logs/                       # Логи приложения
```

## 🎮 Команды бота

### Для пользователей:
- `/start` - Начать работу с ботом, главное меню
- `/menu` - Просмотр меню (перенаправляет в главное меню)
- `/orders` - Мои текущие заказы
- `/help` - Справка по использованию бота
- `/cancel` - Отменить текущую операцию

### Для администраторов:
- `/admin` - Админ-панель с расширенными функциями:
  - 📋 Просмотр всех заказов с фильтрацией
  - 📊 Заказы на сегодня
  - 📈 Отчеты и статистика (включая отчеты по кафе)
  - 🍽️ Управление меню
  - 👥 Управление сотрудниками
  - 🏢 Управление офисами и кафе
  - ⏰ Управление дедлайнами
  - 🔍 Статус системы

## ⚙️ Настройка

### Переменные окружения (.env)

- `BOT_TOKEN` - токен бота от @BotFather (обязательно)
- `DATABASE_URL` - URL базы данных (по умолчанию: `sqlite:///./lunch_bot.db`)
- `ADMIN_IDS` - ID администраторов через запятую (обязательно)
- `ORDER_DEADLINE_HOUR` - час дедлайна заказа (по умолчанию: 12)
- `ORDER_DEADLINE_MINUTE` - минута дедлайна заказа (по умолчанию: 0)

### База данных

По умолчанию используется SQLite. Для production рекомендуется PostgreSQL:

```env
DATABASE_URL=postgresql+asyncpg://user:password@localhost/lunch_bot
```

## 🔧 Разработка

### Технологии

- **aiogram 3.4.1** - асинхронный фреймворк для Telegram ботов
- **SQLAlchemy 2.0** - ORM для работы с БД
- **aiosqlite** - асинхронный драйвер для SQLite
- **APScheduler** - планировщик задач для напоминаний
- **openpyxl** - работа с Excel файлами
- **loguru** - логирование
- **pytest** - фреймворк для тестирования

### Запуск в режиме разработки

```bash
python main.py
```

Логи сохраняются в папке `logs/` с ротацией по дням.

### Запуск тестов

```bash
pytest
```

Или с подробным выводом:
```bash
pytest -v
```

Тесты покрывают:
- Валидацию данных
- Форматирование вывода
- Модели базы данных

## 📊 Функционал

### Для пользователей:
1. **Создание заказа:**
   - Выбор даты заказа (до 7 дней вперед)
   - Просмотр меню по категориям
   - Выбор блюд и количества
   - Корзина с возможностью добавления нескольких блюд
   - Подтверждение и оформление заказа

2. **Управление заказами:**
   - Просмотр текущих заказов
   - Детальная информация о заказе
   - Редактирование заказа (добавление/удаление позиций)
   - Отмена заказа (до дедлайна)

3. **История:**
   - Просмотр истории всех заказов
   - Фильтрация по статусу

### Для администраторов:
1. **Управление заказами:**
   - Просмотр всех заказов с фильтрацией
   - Заказы на конкретную дату
   - Детальная статистика
   - Изменение статуса заказов
   - Редактирование заказов

2. **Отчеты и статистика:**
   - Сводка заказов на дату
   - **Отчеты по кафе** - группировка заказов по кафе с детальной информацией о сотрудниках
   - Статистика по блюдам (топ, количество, выручка)
   - Статистика по пользователям (активность, средний чек)
   - Экспорт отчетов в Excel и CSV (включая отчеты по кафе)

3. **Управление меню:**
   - Просмотр списка блюд
   - Добавление, редактирование и удаление блюд
   - Загрузка меню на дату для каждого кафе

4. **Управление сотрудниками:**
   - Загрузка сотрудников (Telegram-ID, имя, офис)
   - Просмотр списка всех сотрудников
   - Назначение офисов сотрудникам

5. **Управление офисами и кафе:**
   - Добавление и редактирование офисов
   - Добавление и редактирование кафе
   - Привязка кафе к офисам
   - Управление активностью офисов и кафе

6. **Управление дедлайнами:**
   - Настройка дедлайнов для конкретных дат
   - Привязка дедлайнов к офисам и кафе
   - Управление активностью дедлайнов

## 🔔 Уведомления

- Автоматические напоминания о дедлайне заказа (за 1 час и за 30 минут)
- Уведомления администраторам о новых заказах
- Подтверждения создания и изменения заказов

## 🛠️ Развертывание

### Бесплатное развертывание

Подробная инструкция по бесплатному развертыванию на Railway, Render, Fly.io и других платформах находится в файле [DEPLOY.md](DEPLOY.md).

**Быстрый старт на Railway:**
1. Создайте аккаунт на https://railway.app
2. Создайте новый проект из GitHub репозитория
3. Добавьте PostgreSQL сервис
4. Настройте переменные окружения (BOT_TOKEN, ADMIN_IDS и др.)
5. Дождитесь автоматического деплоя

### Production настройки

1. Используйте PostgreSQL вместо SQLite
2. Настройте переменные окружения
3. Настройте логирование в файлы
4. Используйте systemd или supervisor для автозапуска (для VPS)
5. Настройте резервное копирование БД

### Пример systemd service (для VPS)

```ini
[Unit]
Description=Lunch Bot
After=network.target

[Service]
Type=simple
User=your_user
WorkingDirectory=/path/to/TGBot
Environment="PATH=/path/to/venv/bin"
ExecStart=/path/to/venv/bin/python main.py
Restart=always

[Install]
WantedBy=multi-user.target
```

## 📝 Лицензия

Проект создан для кейса Cyber Garden School

## 🤝 Поддержка

При возникновении проблем проверьте:
1. Правильность настройки `.env` файла
2. Наличие токена бота
3. Логи в папке `logs/`
4. Инициализацию БД (`python scripts/init_db.py`)

